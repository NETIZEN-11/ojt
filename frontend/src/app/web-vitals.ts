'use client';

/**
 * Web Vitals - DISABLED
 * TODO: Re-enable once web-vitals package is properly configured
 * Current issue: VM errors with startTime property
 */

// Initialize with error handling (DISABLED)
export function initWebVitals() {
  // Web vitals disabled to prevent console errors
  // The errors are coming from Next.js internal web-vitals code
  // and are non-critical but pollute the console
  return;
}
